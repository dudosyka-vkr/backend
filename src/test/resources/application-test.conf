ktor {
    application {
        modules = [org.eyetracker.ApplicationKt.module]
    }
    deployment {
        port = 0
    }
}

jwt {
    secret = "test-secret-key-for-testing-only"
    issuer = "http://localhost/"
    audience = "http://localhost/"
    realm = "Test API"
}

storage {
    uploadDir = "build/test-uploads"
}

database {
    url = "TO_BE_SET_BY_TESTCONTAINERS"
    driver = "org.postgresql.Driver"
    user = "TO_BE_SET_BY_TESTCONTAINERS"
    password = "TO_BE_SET_BY_TESTCONTAINERS"
}
